# Configuration Management

This directory contains configuration and settings management:

- `settings.ts` - SettingsStore for managing user preferences and API keys in localStorage
- Settings are persisted client-side and include:
  - OpenAI API key
  - Anthropic API key
  - Default LLM provider
  - Theme preferences
  - Language preferences
