# Core Analysis Engine

This directory contains the core logic for prompt analysis, including:

- `anatomyParser.ts` - Pattern matching and component detection
- `scorer.ts` - Multi-dimensional scoring system
- `patterns.ts` - Pattern loader and management
- `patterns/` - Language-specific pattern definitions (EN, PT, ES)

These modules replace the backend Python implementation with client-side TypeScript.
